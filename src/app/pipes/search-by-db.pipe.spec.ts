import { SearchByDbPipe } from './search-by-db.pipe';

describe('SearchByDbPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByDbPipe();
    expect(pipe).toBeTruthy();
  });
});
